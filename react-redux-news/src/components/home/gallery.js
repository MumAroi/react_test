import React from 'react';
import { Link } from 'react-router-dom';

const Gallery = (props) => {
    // console.log(props)
    return(
        <div>
            qwe
        </div>
    );
}

export default Gallery;